/**
 * Класс компонента саджеста
 */
export default class Suggest {
    #parent;
    #rows;

    /**
     * 
     */
    constructor(parent, rows) {
        this.#parent = parent;
        this.#rows = rows;
    }

    render() {
        
    }
}